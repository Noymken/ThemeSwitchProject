import { theme as lightTheme } from "./lightTheme.js";
import { theme as darkTheme } from "./darkTheme.js";

export const themes = {
  dark: darkTheme,
  light: lightTheme,
};

export const themesKeys = Object.keys(themes);
